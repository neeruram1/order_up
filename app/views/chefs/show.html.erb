<h1><%= @chef.name %></h1>
<%= link_to "Ingredients", "/chef/#{@chef.id}/ingredients" %>

<section class="popular-ingredients">
  <h2>Three Most Popular Ingredients:</h2>
  <%= @chef.popular_ingredients.each do |ingredient| %>
  <p><%= ingredient.name %> </p>
  <% end %>
</section>
